[package]
name = "zeronix"
version = "0.1.0"
edition = "2024"

[dependencies]
multiboot2 = { version = "0.24.1", default-features = false }
bitflags = "2.13.0"

[[bin]]
name = "zeronix"
test = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
